#import "template.typ": report, exp-title, exp-section, three-line-table

#show: report.with(
  title: "编译原理实验报告",
  experiments: ("实验1：Lex基础词法分析", "实验2：Lex字符串识别"),
  members: (("CZH", "2302XXXX"),),
  date: "2026年4月27日",
)

// ============================================================
//  实验1
// ============================================================

#exp-title("实验1：Lex基础词法分析")

#exp-section(1, "实验目的")

熟悉 educoder 在线实验环境的使用，学习使用 Lex 写简单的词法分析程序，会在 educoder 在线实验环境下使用 Flex 编写调试 Lex 写的程序。

#exp-section(2, "实验内容")

编写一个词法分析程序，它能对输入的一行字符流进行词法分析，依次识别出其中的关系运算符、圆括号和其他符号，并输出对应的记号序列。例如，若输入的字符流为：

```
if (a<=b) a=b
```

则输出：

```
(OTHER,3)(BRACKETS,"(")(OTHER,1)(RELOP,"<=")(OTHER,1)(BRACKETS,")")(OTHER,2)(RELOP,"=")(OTHER,1)
```

其中 `(OTHER,3)` 表示一个非关系运算符、非括号的子串，3 表示长度，长度统计时可能含有空格等空白符。右侧编辑器中的代码已经可以识别出关系运算符和其他符号，需要补充代码使其能够识别圆括号并输出。

#exp-section(3, "实验要求")

在已有代码基础上补充圆括号的识别逻辑，使词法分析器能够正确输出 `BRACKETS` 记号。

#exp-section(4, "实验过程及重点内容")

本实验涉及的记号类型如 @tab:token-types 所示。

#figure(
  three-line-table(
    columns: 3,
    [记号类型], [正规定义], [匹配示例],
    [RELOP], [`<= | < | == | != | >= | >`], [`<=`],
    [BRACKETS], [`\( | \)`], [`(`],
    [OTHER], [`([^<=>()])+`], [`abc`],
  ),
  caption: [实验1涉及的记号类型],
) <tab:token-types>

*1. 添加括号的 Token Type*

在定义区添加宏定义：

```c
#define BRACKETS 3
```

*2. 添加括号的正规定义*

```
brackets    \(|\)
```

*3. 修改 other 的正规定义*

```
other       ([^<=>()])+
```

需要在 `other` 的字符集中去除左右括号，因为括号需要被单独匹配。

*4. 添加 brackets 的输出*

```c
case BRACKETS: fprintf(yyout, "(BRACKETS,\"%s\")", yytext); break;
```

#exp-section(5, "实验结果")

测试输入 `if (a<=b) a=b` 后，程序正确输出了包含 `BRACKETS` 记号的完整记号序列，各记号类型与属性值均与预期一致，测试通过。运行结果如 @fig:exp1-result 所示。

#figure(
  image("example.jpg", width: 50%),
  caption: [实验1测试运行结果],
) <fig:exp1-result>

#exp-section(6, "实验中遇到的问题、难点及解决方案")

*1. 左括号和右括号需要通过 `|` 分割*

若干个正规式匹配同一条语义动作时（如 `(` 和 `)`），需要通过 `|` 来连接，而不能分别写两条规则。

*2. 最长匹配导致无法仅靠优先级识别括号*

一个错误思路是：将 `brackets` 的匹配放在 `other` 之前，通过优先级完成括号的匹配。但由于 Lex 的最长匹配性质，例如输入 `(ab)` 时，Lex 不会将 `(` 单独匹配，而是匹配整个 `(ab)` 这个长度为 4 的串。因此必须在 `other` 的规则中去除左右括号。

#exp-section(7, "感想和收获")

通过本次实验初步掌握了 Lex 的基本语法结构（定义区、规则区、用户代码区）和 Flex 的编译调试流程。认识到词法分析器中正规定义的设计需要格外注意字符集的互斥性，最长匹配原则会影响规则的实际行为，不能仅靠规则顺序来解决冲突。

#exp-section(8, "小组分工情况")

本次为个人实验，所有内容均独立完成。

// ============================================================
//  实验2
// ============================================================

#exp-title("实验2：Lex字符串识别")

#exp-section(1, "实验目的")

熟悉在线开发环境的使用，学习使用 Lex 写简单的词法分析程序，会使用 Flex 调试 Lex 写的程序。在实验一的基础上进一步扩展词法分析器的功能。

#exp-section(2, "实验内容")

在实验一所改写的程序基础上增加识别 `STRING` 记号。`STRING` 是字符串：若 `"` 出现在字符串中，则必须转义写成 `\"`；若 `\` 出现在字符串中，也必须转义写成 `\\`。

#exp-section(3, "实验要求")

在右侧编辑器补充代码，实现对输入文本中 `STRING` 记号的识别，设计识别 `STRING` 记号的词法分析器。

#exp-section(4, "实验过程及重点内容")

*1. 扩充 Token Type*

```c
#define STRING 20
```

*2. 编写正规定义*

```
normalch    [A-Za-z0-9]
esc         \\([\"\\n'0a])
string      \"({normalch}|{esc})*\"
```

设计思路是将字符串内部允许出现的内容划分为"普通字符"和"转义字符"两类：
- `normalch` 定义为 `[A-Za-z0-9]`，表示字符串中可以直接出现的普通字符仅限字母和数字。
- `esc` 定义为 `\\([\"\\n'0a])`，描述合法转义形式，包括 `\"`、`\\`、`\n`、`\'`、`\0`、`\a`。
- `string` 定义为 `\"({normalch}|{esc})*\"`，即以双引号开头和结尾，中间可以包含零个或多个普通字符或合法转义序列。

*3. 添加规则翻译部分*

```
<INITIAL>{string}           { return STRING; }
```

*4. 添加输出*

```c
case STRING: fprintf(yyout, "(STRING, %s) ", yytext); break;
```

由于匹配到的词素已经包含双引号，所以输出时不再额外加引号。

#exp-section(5, "实验结果")

// 同样可以用 #image("path/to/img.png") 插入截图
程序能够正确识别合法字符串（如 `"hello"`、`"say\"hi"`）并输出 `STRING` 记号，同时对非法字符串（如包含未转义单引号的串）不会误识别，测试通过。

#exp-section(6, "实验中遇到的问题、难点及解决方案")

*1. 规则区顶格注释导致 unrecognized rule*

在规则区中，顶格书写的每一行都会被优先当作规则解析。因此类似以下写法会被误认为是一条规则：

```lex
/* 处理块注释 */
<INITIAL>"/*" { BEGIN COMMENT; }
```

解决方案：将注释加缩进，或将注释写在动作代码块内部。

*2. 字符串规则过宽导致非法串被误识别*

最初采用宽松的通用规则 `\"([^\"\\\n]|\\.)*\"`，虽然能识别一般形式的字符串，但也会把不应识别的输入（如 `"string8'"` 中的未转义单引号）整体匹配为 `STRING`。解决方案是收紧规则，只允许字母数字作为普通字符，并严格限定合法转义序列。

#exp-section(7, "感想和收获")

通过本次实验，进一步理解了 Lex 规则区的语法特点和词法分析器的匹配机制，认识到规则设计必须严格依据实验要求，不能仅凭一般经验书写。同时也加深了对 `yytext` 与记号属性值区别的理解。实验过程中不断根据错误结果调整规则，使编译原理中的词法分析思想更加具体、清晰。

#exp-section(8, "小组分工情况")

本次为个人实验，所有内容均独立完成。
